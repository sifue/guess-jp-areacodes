{
  "name": "guess-jp-areacodes",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "data:generate": "node scripts/generate-data.js",
    "format": "prettier --write src scripts tests index.html package.json playwright.config.js wrangler.jsonc docs README.md",
    "format:check": "prettier --check src scripts tests index.html package.json playwright.config.js wrangler.jsonc docs README.md"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  }
}
